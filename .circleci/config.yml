version: 2.1

jobs:
  release:
    macos:
      # https://circleci.com/docs/2.0/testing-ios/
      xcode: 11.5.0
    steps:
      - checkout

workflows:
  version: 2
  build:
    jobs:
      - release:
          context: open-source
          filters:
            branches:
              only: /^dummy.*/
